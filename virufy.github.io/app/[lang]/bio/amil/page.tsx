import RedirectWithSpinner from '../../components/RedirectWithSpinner';

const AmilBioPage = () => {
  return <RedirectWithSpinner targetUrl={'https://tiny.cc/amil-bio'} />;
};

export default AmilBioPage;
